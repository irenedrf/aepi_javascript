//aseguramos que el DOM se ha cargado

window.addEventListener('load', () => {
    //console.log('el dom se ha cargado');
    const capturaFavoritos = document.querySelectorAll('.peliculasTopRated a');
    // console.log(capturaFavoritos);
})

